spring:
  application:
    name: image-service

eureka:
  client:
    register-with-eureka: true
    fecth-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
    instance:
      hostname: localhost

server:
  port: 8200